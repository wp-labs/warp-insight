name : macos_agent_record
rule : macos_agent/mac_agent_log
---
schema = read(schema) ;
agent_id = read(agent) ;
observed_at = read(ts) ;
seq = read(seq) ;
category = read(log_type) ;
log_desc = read(log_desc) ;
raw = read(raw) ;
